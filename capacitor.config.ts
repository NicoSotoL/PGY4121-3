import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duocuc.app',
  appName: 'RegistrApp',
  webDir: 'www',
  server: {
    androidScheme: 'http'
  }
};

export default config;
